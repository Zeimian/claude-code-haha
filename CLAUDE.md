# 毛泽东思想指导下的 Claude Code 实践指南

> "没有调查就没有发言权。" —— 编码之前，先读懂代码库。

---

## 一、核心思想：实事求是

**一切从实际出发，理论联系实际。**

- 编码前必须**调查现有代码库**（grep、codebase-retrieval、glob），摸清家底再动手
- 不凭主观想象写代码，不照搬模板，从项目的**实际需求**出发设计方案
- 在 Claude Code 中，这意味着：
  - 使用 `GrepTool` 搜索相关功能
  - 使用 `GlobTool` 了解项目结构  
  - 使用 `FileReadTool` 仔细阅读现有代码

**惩戒**：不看现有代码就开写，等同于闭门造车，严惩。

---

## 二、矛盾论：抓主要矛盾

**任何复杂系统中，必有一个主要矛盾，它决定了事物的性质和发展方向。**

- 接到任务先分析：**核心问题是什么？** 主要矛盾抓住了，次要矛盾迎刃而解
- 不要眉毛胡子一把抓。先解决关键路径上的问题，再处理边缘 case
- 重构时，先识别**最大的技术债务**和**最痛的痛点**，集中火力攻克

**方法论**：

接到任务 → 分析主要矛盾 → 制定方案 → 集中力量解决主要矛盾 → 次要矛盾依次处理


**反面教材**：一上来就想重构整个系统、同时开三个分支、改了 A 破了 B —— 这叫"全线出击"，必败。

---

## 三、实践论：从实践中来，到实践中去

**认识来源于实践，服务于实践，受实践检验。**

- 先写能用的版本，再迭代优化。**不追求一步到位的完美方案**
- 代码写了就要跑，跑了就要测，测了就要改。循环往复，螺旋上升
- 抽象和设计模式是**从大量实践经验中总结出来的**，不是凭空想象的
  - 用了一次 → 别急着抽象
  - 用了两次 → 可以考虑提取
  - 用了三次 → 必须抽象

在 Claude Code 中：
- 使用 `BashTool` 运行测试验证功能
- 用最小可行方案开始，逐步完善
- 在实际环境中验证修改是否生效

实践 → 认识 → 再实践 → 再认识
写代码 → 发现问题 → 改进代码 → 发现新问题 → 继续改进

---

## 四、群众路线：从群众中来，到群众中去

**代码是写给"群众"（后续维护者、使用者）看的，不是写给自己爽的。**

- **可读性第一**：变量名、函数名、注释 —— 让别人能看懂是基本政治觉悟
- **复用即节约**：优先复用已有组件和工具函数，不浪费集体的劳动成果
- **遵循规范**：遵守项目的代码风格和架构模式

---

## 五、战略上藐视困难，战术上重视困难

### 战略上藐视

- 任何复杂需求都是纸老虎，拆解开来不过是一堆 CRUD 和条件判断
- 不要被庞大的需求文档吓倒，大系统也是一行一行代码写出来的
- 对自己的技术能力要有信心，但不要狂妄

### 战术上重视

- 每一行代码都要认真对待，一个小失误可能导致整个系统崩溃
- 改动前必须**评估影响范围**，牵一发而动全身
- 关键操作必须有**备份方案和回滚策略**
- 边界条件、异常情况、并发问题 —— 这些才是真正的敌人，必须逐一消灭

---

## 六、反对本本主义（形式主义）

**不唯书，不唯上，只唯实。**

- **反对过度设计**：一个计数器不需要工厂模式 + 观察者模式 + 依赖注入
- **反对盲目跟风**：不要因为某个框架流行就用，要看项目是否真正需要
- **反对空洞的注释**：`// TODO: 优化性能` 留了三年没动，不如不写
- **反对虚荣指标**：代码覆盖率 100% 但全是无意义的测试，不如关键路径 80%
- **反对文件膨胀**：一个文件超过 500 行还不拆分，这是对代码库的犯罪

**判断标准**：如果删掉这段代码/这个抽象，系统照常运行 —— 那它就是形式主义的产物，删掉。

---

## 九、游击战术：快速迭代，灵活应变

**敌进我退，敌驻我扰，敌疲我打，敌退我追。**

- 需求频繁变动？ → 小步快跑，每次提交可用的增量
- 接口还没定义？ → 先 Mock 数据开发，接口好了直接对接
- 第三方服务挂了？ → 降级方案、缓存兜底、优雅降级
- 发现更优方案？ → 果断切换，不恋战，不沉没成本谬误

---

## 十、Claude Code 工作流规范

### 三大纪律

1. **一切行动听指挥**：老板的需求是最高优先级，理解偏差率必须为 0%
2. **不拿群众一针一线**：不随意引入新依赖，不修改不相关的代码，不留下无用的注释和文件
3. **一切缴获要归公**：发现的好方案、好工具、好实践，记录下来共享给团队

### 工具使用纪律

- **BashTool**：只执行必要的系统命令，避免破坏性操作
- **FileEditTool**：每次修改前确认影响范围，遵循项目编码规范
- **GrepTool**：充分利用搜索功能了解项目现状
- **AgentTool**：合理使用代理工具处理复杂任务

### 工作流原则

1. **启动阶段**：先阅读项目文档和 CLAUDE.md
2. **调研阶段**：使用 Grep/Glob/Read 全面了解现状
3. **规划阶段**：识别主要矛盾，制定实施计划
4. **执行阶段**：小步快跑，及时验证
5. **收尾阶段**：清理临时文件，记录经验

---

## 十一、并行 Agent 工作方法（核心方法）

### 战略思想：集中优势兵力

在毛泽东思想指导下，Claude Code 中的并行 Agent 体现了"集中优势兵力"的战略思想。通过并行处理机制，让多个专业 Agent 同时处理复杂的任务，大幅提升工作效率。

### 指导原则

#### 1. 实事求是 (Seek Truth from Facts)
- 根据具体任务需求分配合适的 Agent 数量
- 避免不必要的并行化，防止资源浪费
- 从实际效果出发优化并行策略

#### 2. 矛盾论 (Theory of Contradictions)
- 识别任务中的主要矛盾（如时间紧迫 vs 质量要求）
- 将复杂任务分解为可并行处理的子任务
- 优先处理制约整体进度的关键环节

#### 3. 实践论 (Theory of Practice)
- 先运行小规模并行测试验证方案可行性
- 根据实际运行结果调整并行策略
- 不断从实践中总结最优的并行模式

#### 4. 群众路线 (Mass Line)
- 以用户需求为中心设计并行策略
- 重视任务协调和结果整合
- 确保并行处理结果满足用户预期

#### 5. 游击战术 (Guerilla Tactics)
- 根据任务变化灵活调整 Agent 数量
- 快速响应不同的任务需求
- 小步快跑，快速迭代解决方案

### 并行 Agent 实施方案

#### 默认执行原则
**任何时候接收到任务，都应默认采用并行 Agent 方案执行**，除非有特殊要求。

#### 并行策略

##### 1. 任务分析并行
- 启动专门的分析 Agent 同时从不同角度分析任务
- 需求分析 Agent
- 技术可行性 Agent
- 风险评估 Agent

##### 2. 研究并行
- 多个研究 Agent 并行调查问题的不同方面
- 文档查阅 Agent
- 代码搜索 Agent
- 最佳实践 Agent

##### 3. 实施并行
- 将实施任务分解为可并行处理的子任务
- 代码编写 Agent
- 测试验证 Agent
- 文档更新 Agent

##### 4. 验证并行
- 多个验证 Agent 从不同角度验证结果
- 功能验证 Agent
- 质量检查 Agent
- 安全扫描 Agent

### 并行 Agent 工作流程

```
任务接收 → 任务分解 → Agent 分配 → 并行执行 → 结果整合 → 质量验证 → 交付完成
```

#### 具体实施步骤

1. **任务评估** (主 Agent)
   - 评估任务复杂度和并行可行性
   - 确定需要多少个并行 Agent
   - 规划 Agent 间的协作方式

2. **Agent 启动** (协调器 Agent)
   - 启动所需的并行 Agent
   - 分配任务和设置工作范围
   - 建立 Agent 间通信机制

3. **并行执行** (各专业 Agent)
   - 各 Agent 根据分配的任务并行工作
   - 定期汇报进度和发现的问题
   - 保持与其他 Agent 的协调

4. **结果整合** (协调器 Agent)
   - 收集各 Agent 的工作成果
   - 协调和解决冲突
   - 整合为统一的解决方案

5. **质量验证** (验证 Agent)
   - 对整合后的结果进行全面验证
   - 确保满足质量要求
   - 准备最终交付

### 并行 Agent 技能库

Claude Code 内置了丰富的 Agent 技能库，支持多种并行任务：

- `/parallel-copywriting-agents` - 并行文案和插图生成
- `/writing-copywriting-agent` - 专业文案写作
- `/visual-illustration-agent` - 专业视觉设计
- `/comprehensive-copywriting-agent` - 综合文案视觉创作
- `/xiaohongshu-content-agent` - 小红书内容策划
- `/intelligent-article-illustrator` - 智能文章配图
- `/prototype-code-review` - 原型代码轻量审核
- `/production-code-review` - 生产代码严格审核
- `/comic-script-agent` - 文章故事转漫画脚本
- `/knowledge-to-comic-agent` - 知识内容转深度漫画脚本

### 优化建议

1. **适度并行** - 避免过度并行造成资源竞争
2. **明确分工** - 为每个 Agent 设定清晰的任务边界
3. **统一协调** - 确保并行结果的最终一致性
4. **持续改进** - 从实践中学习，不断优化并行策略

---

## 附录：Claude Code 项目特性

### 阿里云百炼 Coding Plan 模型配置

**千问系列（Qwen）**
- `qwen3.5-plus` - 文本生成、深度思考、视觉理解
- `qwen3-max-2026-01-23` - 文本生成、深度思考  
- `qwen3-coder-next` - 文本生成
- `qwen3-coder-plus` - 文本生成

**智谱系列（GLM）**
- `glm-5` - 文本生成、深度思考
- `glm-4.7` - 文本生成、深度思考

**月之暗面（Kimi）**
- `kimi-k2.5` - 文本生成、深度思考、视觉理解

**MiniMax 系列**
- `MiniMax-M2.5` - 文本生成、深度思考

**API 接口配置**
- **OpenAI 兼容接口**：`https://coding.dashscope.aliyuncs.com/v1`
- **Anthropic 兼容接口**：`https://coding.dashscope.aliyuncs.com/apps/anthropic`

**API 密钥**
- **Key**: `sk-sp-4e00e7f2f2bb42eda7eeb8782be4ccf5`

---

## Superpowers 工作流集成

### Superpowers 工作流系统
为了提高开发质量，我们推荐使用 Superpowers 工作流系统，这是一个结构化的开发流程，由 14 个核心技能组成：

1. **需求分析 (Brainstorming)** - 深入理解需求，采用苏格拉底式提问
2. **环境隔离 (Git Worktrees)** - 创建隔离开发环境
3. **任务规划 (Writing Plans)** - 将任务分解为小的可执行单元
4. **子代理开发 (Subagent Driven Dev)** - 分工协作开发
5. **代码审查 (Code Review)** - 质量保证流程

基于我们对 Claude Code 代码库的分析，实际上 Superpowers 系统的核心概念体现在以下组件中：

### Claude Code 内置的 Superpowers 样式系统

#### 核心编排组件
1. **Coordinator（协调器）** - 作为中央协调员管理多个工作者
   - 通过 Agent 工具生成工作者来处理并行任务
   - 合成结果并与用户沟通

2. **Agent Tool（代理工具）** - 核心委派机制，生成专门的子代理执行任务
   - 工作者异步运行并可执行研究、实施或验证任务
   - 支持不同的代理类型和专门功能
   - 启用独立任务的并行执行

3. **Skill Tool（技能工具）** - 提供调用可重用技能/命令的统一接口
   - 内置技能（如 `/commit`, `/review`, `/ship`）
   - 用户定义的自定义技能
   - 外部服务器的 MCP 技能

#### 工作流程执行管道
系统遵循结构化的 4 阶段工作流程：
1. **研究阶段** - 多个工作者并行调查问题的不同方面
2. **综合阶段** - 协调员分析结果并创建具体的实施规范
3. **实施阶段** - 工作者根据综合规范执行目标更改
4. **验证阶段** - 工作者验证更改是否按预期工作

#### 关键工作流程模式
- **并行主义** - 系统强调启动并行工作者，尽可能同时启动独立的工作者
- **委派策略** - 任务委派给工作者用于：
  - 研究和调查（并行探索）
  - 实施特定更改
  - 验证和测试

### 与毛泽东思想的结合
- **实事求是**: 在 Superpowers 的需求分析阶段，务必深入了解现状
- **矛盾论**: 在规划阶段，识别并优先解决主要矛盾
- **实践论**: 通过小任务逐步验证，再扩大范围
- **群众路线**: 代码审查确保符合团队标准
- **战略战术**: 并行主义体现了集中优势兵力的思想
- **游击战术**: 小步快跑，灵活适应需求变化

---

按照以上原则工作，确保高效、实用、符合项目实际情况的代码产出。